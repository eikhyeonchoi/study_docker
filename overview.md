# Docker
## Overview
```
하나의 운영체제(한 개 = Docker HOST)에 원하는 app을 올릴 수 있다
각각의 app은 container단위로 실행되며 독립적으로 실행된다
따로 운영체제가 필요한게 아닌 lib, bin만 필요로 한다

app store == docker hub
program == image
proces == container
docker hub -> image 다운로드 == pull
image 실행 == run (image->container)
```